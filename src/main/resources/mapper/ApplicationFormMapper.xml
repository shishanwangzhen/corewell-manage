<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//www.mybatis.org//DTD Mapper 3.0//EN"
        "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.corewell.corewellmanage.dao.ApplicationFormDao">
    <resultMap id="ApplicationFormMap" type="com.corewell.corewellmanage.domain.ApplicationForm">
        <id property="id" column="id"/>

        <result property="number" column="number"/>
        <result property="projectId" column="project_id"/>
        <result property="warehouseId" column="warehouse_id"/>
        <result property="managerId" column="manager_id"/>
        <result property="financeId" column="finance_id"/>
        <result property="status" column="status"/>
        <result property="notes" column="notes"/>
        <result property="creatorId" column="creator_id"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="getApplicationForm" parameterType="ApplicationFormParam" resultType="ApplicationForm">
        select id,number,project_id as projectId, warehouse_id as warehouseId, manager_id as managerId,
        finance_id as financeId, status ,notes,creator_id as creatorId, create_time as createTime
        from application_form
        where 1=1
        <if test="id!=null and id!=''">
            and id=#{id}
        </if>
        <if test="name!=null and name!=''">
            and name like CONCAT('%' , #{name ,jdbcType=VARCHAR},'%')
        </if>
    </select>

    <insert id="addApplicationForm" parameterType="ApplicationFormAddParam">
        insert into application_form
        <trim prefix="(" suffix=")" suffixOverrides=",">

            <if test="number!=null and number!=''">
                number,
            </if>
            <if test="projectId!=null and projectId!=''">
                project_id,
            </if>

            <if test="warehouseId!=null and warehouseId!=''">
                warehouse_id,
            </if>
            <if test="managerId!=null and managerId!=''">
                manager_id,
            </if>
            <if test="financeId!=null and financeId!=''">
                finance_id,
            </if>
            <if test="status!=null and status!=''">
                status,
            </if>
            <if test="notes!=null and notes!=''">
                notes,
            </if>
            <if test="createTime!=null">
                create_time,
            </if>
            <if test="creatorId!=null and creatorId!=''">
                creator_id
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="number!=null and number!=''">
                #{number},
            </if>
            <if test="projectId!=null and projectId!=''">
                #{projectId},
            </if>
            <if test="warehouseId!=null and warehouseId!=''">
                #{warehouseId},
            </if>
            <if test="managerId!=null and managerId!=''">
                #{managerId},
            </if>
            <if test="financeId!=null and financeId!=''">
                #{financeId},
            </if>
            <if test="status!=null and status!=''">
                #{status},
            </if>
            <if test="notes!=null and notes!=''">
                #{notes},
            </if>
            <if test="createTime!=null">
                #{createTime},
            </if>
            <if test="creatorId!=null and creatorId!=''">
                #{creatorId}
            </if>
        </trim>
    </insert>


    <update id="updateApplicationForm" parameterType="ApplicationFormUpdateParam">
        update application_form
        <set>
            <if test="number!=null and number!=''">
                number=#{number},
            </if>
            <if test="projectId!=null and projectId!=''">
                project_id=#{projectId},
            </if>

            <if test="warehouseId!=null and warehouseId!=''">
                warehouse_id =#{warehouseId},
            </if>
            <if test="managerId!=null and managerId!=''">
                manager_id =#{managerId},
            </if>
            <if test="financeId!=null and financeId!=''">
                finance_id =#{financeId},
            </if>
            <if test="notes!=null and notes!=''">
                notes =#{notes},
            </if>
            <if test="status!=null and status!=''">
                status =#{status}
            </if>

        </set>
        where id=#{id}
    </update>

    <update id="deleteApplicationForm" parameterType="Long">
        delete
        from application_form
        where id = #{id}
    </update>
</mapper>

