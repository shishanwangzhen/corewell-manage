<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//www.mybatis.org//DTD Mapper 3.0//EN"
        "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.corewell.corewellmanage.dao.AccountDao">
    <resultMap id="AccountMap" type="com.corewell.corewellmanage.domain.Account">
        <id property="id" column="id"/>
        <result property="account" column="account"/>
        <result property="name" column="name"/>
        <result property="password" column="password"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="sex" column="sex"/>
        <result property="role" column="role"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectAccount" parameterType="String" resultType="AccountDTO">
        select id, account, password, name
        from account
        where account = #{account}
    </select>

    <select id="getAccount" parameterType="AccountParam" resultType="Account">
        select id, account, name,password, phone, email,sex, role,
        create_time as createTime, update_time as updateTime
        from account
        where del_flag=1
        <if test="id!=null and id!=''">
            and id=#{id}
        </if>
        <if test="account!=null and account!=''">
            and account=#{account}
        </if>
        <if test="name!=null and name!=''">
            and name like CONCAT('%' , #{name ,jdbcType=VARCHAR},'%')
        </if>
        <if test="role!=null and role!=''">
            and role=#{role}
        </if>
        order by create_time desc
    </select>

    <insert id="addAccount" parameterType="AccountAddParam">
        insert into account
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="account!=null and account!=''">
                account,
            </if>
            <if test="password!=null and password!=''">
                password,
            </if>
            <if test="name!=null and name!=''">
                name,
            </if>
            <if test="phone!=null and phone!=''">
                phone,
            </if>
            <if test="email!=null and email!=''">
                email,
            </if>
            <if test="sex!=null and sex!=''">
                sex,
            </if>
            <if test="createTime!=null">
                create_time,
            </if>
            <if test="role!=null and role!=''">
                role
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">

            <if test="account!=null and account!=''">
                #{account},
            </if>
            <if test="password!=null and password!=''">
                #{password},
            </if>
            <if test="name!=null and name!=''">
                #{name},
            </if>
            <if test="phone!=null and phone!=''">
                #{phone},
            </if>
            <if test="email!=null and email!=''">
                #{email},
            </if>
            <if test="sex!=null and sex!=''">
                #{sex},
            </if>
            <if test="createTime!=null">
                #{createTime},
            </if>
            <if test="role!=null and role!=''">
                #{role}
            </if>
        </trim>
    </insert>


    <update id="updateAccount" parameterType="AccountUpdateParam">
        update account
        <set>
            <if test="account!=null and account!=''">
                account = #{account},
            </if>
            <if test="password!=null and password!=''">
                password =#{password},
            </if>
            <if test="name!=null and name!=''">
                name = #{name},
            </if>

            <if test="phone!=null and phone!=''">
                phone =#{phone},
            </if>
            <if test="email!=null and email!=''">
                email =#{email},
            </if>
            <if test="sex!=null and sex!=''">
                sex =#{sex},
            </if>
            <if test="updateTime!=null">
                update_time =#{updateTime},
            </if>
            <if test="role!=null and role!=''">
                role =#{role}
            </if>

        </set>
        where id=#{id}
    </update>

    <update id="deleteAccount" parameterType="Long">
        update account
        set del_flag=2,
            update_time=now()
        where id = #{id}
    </update>
</mapper>

